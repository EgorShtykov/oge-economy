# 🎮 Система поощрения учеников ОГЭ по математике

Единая система игровой мотивации для учеников, изучающих материал к ОГЭ по математике.

## 📋 Содержание

- [Возможности](#возможности)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Настройка Google Forms](#настройка-google-forms)
- [Добавление новых заданий](#добавление-новых-заданий)
- [Настройка наград](#настройка-наград)
- [API системы поощрения](#api-системы-поощрения)

## ✨ Возможности

### 🪙 Игровая валюта
- **Математические монеты** за правильные ответы
- Разные награды за сложность: лёгкие (5), средние (10), сложные (15)
- Бонусы за серию правильных ответов
- Ежедневные бонусы за посещение

### 📊 Отслеживание прогресса
- Общий прогресс по всем заданиям
- Статистика по каждому типу задания
- Процент выполнения с визуальным индикатором
- История решённых задач

### 🏆 Система достижений
- 8 достижений для мотивации
- Бонусы за разблокировку достижений
- Визуальное отображение прогресса

### 🛒 Магазин призов
- Возможность тратить монеты на призы
- Различные товары: пропуски заданий, подсказки, сертификаты
- Визуальное отображение доступных покупок

### 📤 Отправка результатов
- Интеграция с Google Forms
- Автоматическая отправка прогресса учителю
- Учёт имени ученика и времени выполнения

## 📁 Структура проекта

```
/
├── index.html              # Главная страница (магазин)
├── reward-system.js        # Единая система поощрения
├── template_new_task.html  # Шаблон для новых заданий
├── README.md              # Эта документация
│
├── oge15.html             # Задание 15: Треугольники
├── oge16.html             # Задание 16: Окружность
├── oge_task17.html        # Задание 17: Площади фигур
├── oge_task18.html        # Задание 18: На клетчатой бумаге
└── oge_task19.html        # Задание 19: Геометрические утверждения
```

## 🚀 Быстрый старт

### 1. Размещение файлов

Загрузите все файлы на веб-сервер или хостинг. Все файлы должны находиться в одной директории.

### 2. Настройка Google Forms (опционально)

Для отправки результатов учителю настройте Google Forms по инструкции ниже.

### 3. Открытие сайта

Откройте `index.html` в браузере. Ученикам нужно:
1. Ввести своё имя
2. Перейти к заданиям
3. Решать задачи и зарабатывать монеты
4. Тратить монеты в магазине

## 🔧 Настройка Google Forms

### Шаг 1: Создание формы

1. Перейдите в [Google Forms](https://forms.google.com)
2. Создайте новую форму с названием "ОГЭ Математика - Результаты"
3. Добавьте следующие поля:
   - **Имя ученика** (короткий ответ)
   - **Номер задания** (короткий ответ)
   - **Результат** (короткий ответ)
   - **Получено монет** (короткий ответ)
   - **Время** (короткий ответ)

### Шаг 2: Получение URL для отправки

1. Нажмите кнопку "Отправить" (в правом верхнем углу)
2. Выберите значок "<>" (Встроить в HTML)
3. Скопируйте URL формы из кода
4. URL будет вида: `https://docs.google.com/forms/d/e/ВАШ_ИДЕНТИФИКАТОР/formResponse`

### Шаг 3: Получение ID полей

1. Откройте форму в режиме просмотра
2. Нажмите F12 (Инструменты разработчика)
3. Найдите элементы input с name="entry.XXXXXX"
4. Скопируйте ID для каждого поля

### Шаг 4: Обновление кода

Откройте `reward-system.js` и обновите настройки:

```javascript
const CONFIG = {
    GOOGLE_FORM_URL: 'https://docs.google.com/forms/d/e/ВАШ_ИДЕНТИФИКАТОР/formResponse',
    FORM_FIELDS: {
        name: 'entry.XXXXXXXXX',      // ID поля "Имя ученика"
        task: 'entry.XXXXXXXXX',      // ID поля "Номер задания"
        result: 'entry.XXXXXXXXX',    // ID поля "Результат"
        coins: 'entry.XXXXXXXXX',     // ID поля "Получено монет"
        timestamp: 'entry.XXXXXXXXX'  // ID поля "Время"
    }
};
```

## ➕ Добавление новых заданий

### Использование шаблона

1. Скопируйте `template_new_task.html`
2. Переименуйте в `ogeXX.html` (где XX - номер задания)
3. Замените следующие элементы:
   - `XX` → номер задания
   - `НАЗВАНИЕ ЗАДАНИЯ` → название темы
   - Теоретический материал
   - Задачи с ответами

### Структура задачи

```html
<div class="problem" data-difficulty="easy">
    <div class="problem-header">
        <span class="problem-number">1</span>
        <span class="year">2023 ОГЭ</span>
        <span class="difficulty easy">Лёгкий</span>
    </div>
    <p class="problem-text">Текст задачи...</p>
    <input type="number" class="answer-input" id="ans1" placeholder="Введите ответ">
    <button class="check-btn" onclick="checkAnswer(1, ОТВЕТ, 'easy')">Проверить</button>
    <button class="hint-btn" onclick="toggleHint(1)">Подсказка</button>
    <div class="hint" id="hint1">Подсказка...</div>
    <div class="result" id="result1"></div>
</div>
```

### Уровни сложности

- `easy` — лёгкие (5 монет)
- `medium` — средние (10 монет)
- `hard` — сложные (15 монет)

### Добавление в магазин

В `index.html` добавьте новую карточку в раздел "Перейти к заданиям":

```html
<a href="ogeXX.html" class="task-card">
    <div class="task-number">XX</div>
    <div class="task-name">Название темы</div>
    <div class="task-progress" id="progress-XX">0/0 решено</div>
</a>
```

## 🎁 Настройка наград

### Изменение стоимости наград

В `reward-system.js` измените значения:

```javascript
REWARDS: {
    EASY: 5,      // Лёгкое задание
    MEDIUM: 10,   // Среднее задание
    HARD: 15,     // Сложное задание
    BONUS: 20     // Бонус за серию
}
```

### Добавление новых товаров в магазин

В `index.html` добавьте новый товар:

```html
<div class="shop-item" data-id="my-item">
    <div class="shop-item-icon">🎁</div>
    <div class="shop-item-name">Название товара</div>
    <div class="shop-item-desc">Описание товара</div>
    <div class="shop-item-price">🪙 100 монет</div>
    <button class="buy-btn" onclick="buyItem('my-item', 100)">Купить</button>
</div>
```

Затем добавьте название в объект `itemNames` в JavaScript.

## 📚 API системы поощрения

### Основные методы

#### `RewardSystem.getBalance()`
Возвращает текущий баланс монет.

```javascript
const balance = RewardSystem.getBalance();
console.log(`У тебя ${balance} монет`);
```

#### `RewardSystem.addCoins(amount, reason)`
Начисляет монеты.

```javascript
RewardSystem.addCoins(10, 'Бонус за регистрацию');
```

#### `RewardSystem.spendCoins(amount, reason)`
Списывает монеты. Возвращает `true` если успешно.

```javascript
if (RewardSystem.spendCoins(50, 'Покупка подсказки')) {
    console.log('Покупка совершена!');
}
```

#### `RewardSystem.rewardForTask(difficulty, taskId)`
Начисляет награду за задание.

```javascript
RewardSystem.rewardForTask('medium', 'task15_5');
```

#### `RewardSystem.markTaskSolved(taskId, coins)`
Отмечает задание как решённое.

```javascript
RewardSystem.markTaskSolved('task15_1', 10);
```

#### `RewardSystem.isTaskSolved(taskId)`
Проверяет, решено ли задание.

```javascript
if (RewardSystem.isTaskSolved('task15_1')) {
    console.log('Задание уже решено');
}
```

#### `RewardSystem.getProgress(totalTasks)`
Возвращает объект с прогрессом.

```javascript
const progress = RewardSystem.getProgress(17);
console.log(`${progress.solved}/${progress.total} (${progress.percent}%)`);
```

#### `RewardSystem.setStudentName(name)`
Устанавливает имя ученика.

```javascript
RewardSystem.setStudentName('Иван');
```

#### `RewardSystem.askStudentName()`
Запрашивает имя через prompt (если не установлено).

```javascript
RewardSystem.askStudentName();
```

#### `RewardSystem.submitToGoogleForms(data)`
Отправляет результаты в Google Forms.

```javascript
RewardSystem.submitToGoogleForms({
    task: 'task15_1',
    result: '✓ Верно',
    coins: 10
});
```

#### `RewardSystem.resetAllProgress()`
Сбрасывает весь прогресс (с подтверждением).

```javascript
RewardSystem.resetAllProgress();
```

### События

Система автоматически:
- Показывает уведомления о получении монет
- Обновляет отображение баланса
- Проверяет и показывает достижения
- Начисляет ежедневные бонусы

## 🎨 Кастомизация

### Изменение цветовой схемы

Основные цвета определены в CSS:

```css
/* Основной градиент */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Цвет успеха */
background: #c8e6c9;
color: #2e7d32;

/* Цвет ошибки */
background: #ffcdd2;
color: #c62828;
```

### Изменение иконки валюты

В `reward-system.js`:

```javascript
const CONFIG = {
    CURRENCY_ICON: '🪙',  // Измени на свою иконку
    CURRENCY_NAME: 'монет'
};
```

## 📱 Совместимость

- ✅ Chrome 60+
- ✅ Firefox 55+
- ✅ Safari 12+
- ✅ Edge 79+
- ✅ Мобильные браузеры (iOS Safari, Chrome Android)

## 🔒 Безопасность

- Данные хранятся в localStorage (на устройстве ученика)
- Нет серверной части — всё работает на клиенте
- Для продакшена рекомендуется добавить серверную валидацию

## 🐛 Отладка

Включите консоль разработчика (F12) для просмотра:
- Начисленных монет
- Ошибок отправки в Google Forms
- Состояния localStorage

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль на ошибки
2. Убедитесь, что все файлы загружены
3. Проверьте настройки Google Forms
4. Очистите localStorage и попробуйте снова

## 📄 Лицензия

Свободное использование для образовательных целей.

---

**Сделано с ❤️ для учеников, готовящихся к ОГЭ по математике!**
